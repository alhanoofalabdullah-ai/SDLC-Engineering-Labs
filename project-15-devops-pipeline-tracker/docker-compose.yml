version: "3.9"

services:

  mysql:
    image: mysql:8

    container_name: devops-pipeline-mysql

    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: devops_pipeline_db

    ports:
      - "3306:3306"
