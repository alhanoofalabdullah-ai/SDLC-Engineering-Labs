version: "3.9"

services:

  mysql:

    image: mysql:8

    container_name: enterprise-sdlc-mysql

    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: enterprise_sdlc_db

    ports:
      - "3306:3306"
