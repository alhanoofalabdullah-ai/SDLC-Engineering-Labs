version: "3.9"

services:

  mysql:

    image: mysql:8

    container_name: uat-management-mysql

    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: uat_management_db

    ports:
      - "3306:3306"
