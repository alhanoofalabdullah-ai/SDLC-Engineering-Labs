version: "3.9"

services:

  mysql:
    image: mysql:8

    container_name: quality-dashboard-mysql

    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: quality_dashboard_db

    ports:
      - "3306:3306"
