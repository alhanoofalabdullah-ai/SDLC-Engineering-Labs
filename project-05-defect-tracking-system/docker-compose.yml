version: "3.9"

services:

  mysql:

    image: mysql:8

    container_name: defect-tracking-mysql

    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: defect_tracking_db

    ports:
      - "3306:3306"
