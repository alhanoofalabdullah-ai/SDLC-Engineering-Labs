version: "3.9"

services:
  mysql:
    image: mysql:8
    container_name: waterfall-student-mysql
    restart: always
    environment:
      MYSQL_DATABASE: waterfall_student_db
      MYSQL_ROOT_PASSWORD: password
    ports:
      - "3306:3306"
